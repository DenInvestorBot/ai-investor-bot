services:
  - type: worker
    name: ai-investor-bot
    env: python
    plan: starter
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: python -u main.py
    envVars:
      - key: TZ
        value: Europe/Riga
      - key: LOG_LEVEL
        value: INFO
