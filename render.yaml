services:
  - type: worker
    name: ai-investor-bot
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: TZ
        value: Europe/Riga
      - key: LOG_LEVEL
        value: INFO
